apiVersion: v1
kind: ConfigMap
metadata:
  name: zookeeper-config
data:
  # the zookeeper client port
  ZOOKEEPER_CLIENT_PORT: "2181"
  # the zookeeper tick time for checking heartbeats of kafka brokers and other clients
  ZOOKEEPER_TICK_TIME: "2000"