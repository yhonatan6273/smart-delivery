apiVersion: v1
kind: Service
metadata:
  name: akhq-service
spec:
  #this type will expose the service on each Node's IP at a static port
  type: NodePort
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
  selector:
    app: akhq