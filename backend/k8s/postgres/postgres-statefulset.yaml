apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-statefulset
spec:
  serviceName: "postgres"
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16.4-alpine3.20 
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: app-config
          #this is used for sensitive data from Secrets
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: POSTGRES_PASSWORD
            #set the PGDATA environment variable to specify the data directory
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-storage
            #tells kubernetes to take the disk storage we created and mount it to this path inside the container
              mountPath: /var/lib/postgresql/data
  #creating persistent storage for the Postgres database
  volumeClaimTemplates:
    - metadata:
        name: postgres-storage
      ##specifies the type of access to the volume
      spec:
        #only one node can mount the volume for read and write operations at a time
        accessModes: [ "ReadWriteOnce" ]
        #specifies the amount of resources being requested
        resources:
          requests:
            storage: 1Gi