# This file is for running pytest locally against the test containers

#Test Database Connection (for pytest on host)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test_password
POSTGRES_HOSTNAME=test-db
POSTGRES_PORT=5433
POSTGRES_DB=delivery_db_test
SECRET_KEY=test_secret_key