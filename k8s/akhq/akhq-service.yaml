apiVersion: v1
kind: Service
metadata:
  name: akhq-service
  namespace: delivery-platform
spec:
  selector:
    app: akhq
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: NodePort